name: csv_to_mcq_app
description: Fully integrated optimized exam app with CSV import, scoring, analytics and tests.
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.0
  excel: ^4.0.6
  file_picker: ^10.3.7
  uuid: ^4.5.2
  csv: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
flutter:
  uses-material-design: true
